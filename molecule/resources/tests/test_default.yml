# Molecule managed
---
command:
  help:
    exit-status: 0
    exec: "systemctl is-active ansible-cmdb"
    stdout:
      - "/active/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
